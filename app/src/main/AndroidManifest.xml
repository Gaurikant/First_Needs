<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.exclusive.first_needs">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".start"
            tools:ignore="WrongManifestParent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Register"
            tools:ignore="WrongManifestParent" />
        <activity
            android:name=".login"
            tools:ignore="WrongManifestParent" />
        <activity android:name=".stock1" />
        <activity android:name=".stock2" />
        <activity android:name=".wafers" />
        <activity android:name=".colddrinks" />
        <activity android:name=".namkin" />
        <activity android:name=".maggi" />
        <activity android:name=".choclates" />
        <activity android:name=".biscuits" />
        <activity android:name=".milkproducts" />
    </application>

</manifest>